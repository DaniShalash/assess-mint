import { UserNameComposition } from '@enums';

export type UserNameRule = {
  composition: UserNameComposition;
  minLength: number;
};
// ----------------------
// ------------------------------------------------------------------------------------------
