type APIConfiguration = {
  baseUrl: string;
};
// --------------------

export const APIConfig: APIConfiguration = {
  /**
   * Don't forget the '/' at the end if you want to change the url.
   */
  baseUrl: 'http://localhost:3000/'
};
// -----------------------------------------------------------------------
