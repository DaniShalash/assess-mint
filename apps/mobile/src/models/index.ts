import { GlobalState, AuthState, UserState } from './global-state.model';

export type {
  GlobalState,
  AuthState,
  UserState
};
// ------------------------------------------------------------------------------------------
